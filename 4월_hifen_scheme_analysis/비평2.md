전체 파일을 훑었고(스크립트 970–2355), 1차 수정 반영분과 실제 코드의 간극을 중심으로 감사 결과를 정리했습니다.

1. 총평
수정 후 상태는 "홈이 문서처럼 느껴지고, 품질 뷰가 근거를 드러낸다"는 점에서 분명히 개선됐습니다. 다만 (b) 하드코딩 수치 제거가 서너 군데에서 덜 반영됐고, (i) sticky assumption-bar가 테이블 뷰의 sticky 필터와 y축에서 충돌합니다. 그 외에 1차 때는 품질 뷰의 "근거" 줄이 워낙 돋보여서 가려졌던, 근거 표기 불균형이 이번 라운드에서 가장 눈에 띄는 구조적 결함입니다.

2. 회귀 (Regressions)
하드코딩 수치 잔존 — (b)에 반한 직접 회귀. 아래 5곳은 데이터 바인딩이 아님:
index.html:1439 "...517개 테이블을 좁혀..."
index.html:1708 "257개 사용중 테이블을..."
index.html:1891 "636개의 백엔드 엔드포인트 중..."
index.html:2304 "LOW confidence 엔드포인트 3건"
index.html:2305 "유령 모델 2건"
→ 정작 같은 파일 index.html:1180·2110에서는 zombie_models.length로 바인딩되어 있어, 동일 사실이 두 값으로 표시될 수 있음.
Sticky 계층 충돌 — (i)로 추가된 assumption-bar { position:sticky; top:60px }(index.html:149)의 세로 점유 범위(대략 60–108px)와, 테이블 뷰 filter-panel { top: 80px }(index.html:449)이 겹침. 스크롤 시 필터 패널 상단 약 22–28px가 파란 바 뒤로 들어가 "사용 여부"가 가려짐. 관계 그래프의 .graph-aside는 sticky가 아니라 영향 없음.
(l) 죽은 변수 청소 누락 — index.html:1448의 지역 변수 apps와 index.html:1456의 state.apps(Set)는 현재 UI가 없어 한 번도 쓰이지 않음. 과거 "앱 필터" 제거 시 정리되지 않은 잔재.
3. 새로 드러난 문제
① "가장 방치된 서비스" 카드의 오정렬 위험

(a) index.html:1200–1208에서 uncalled_pct로만 내림차순 → endpoints_total이 1인 소형 앱이 100%로 최상위를 차지할 수 있음.

(b) 홈의 "먼저 확인해볼 것"은 독자가 가장 먼저 클릭할 진입점이라, 여기서 변두리 앱이 튀어나오면 전체 신뢰도가 한 번에 무너짐.

(c) 수정안: filter(a => a.endpoints_total >= N) 하한(예: 5)을 두거나, 정렬 키를 a.endpoints_uncalled - a.endpoints_uncalled*(1-uncalled_pct) 같은 절대량·비율 결합식으로 교체.

② "근거:" 줄의 뷰별 비대칭

(a) index.html:2125 외 품질 6섹션만 evidenceLine을 가짐. 홈 "핵심 소견" 3카드(index.html:1183–1194)와 엔드포인트/관계/서비스 뷰는 같은 양식의 근거 줄이 없음.

(b) 홈은 "전체 API의 N%가 프론트에서 호출되지 않습니다" 같은 가장 강한 주장을 하는 곳인데, 바로 그 주장만 근거 경로가 빠져 있어 "신뢰성" 원칙의 가장 중요한 지점이 비어 있음.

(c) 수정안: home-insights .card 하단에 evidenceLine('core_materials/site_data.json → endpoints[] · intermediate/detect_unused_endpoints.py') 등을 각 카드별로 1줄 추가.

③ About 섹션의 "3.5" 번호

(a) index.html:2284 "3.5. 엔드포인트 호출 스캔의 사각지대". 목차에서 3.5는 "나중에 끼워 넣었음"을 노출하는 인덱스.

(b) 독자(PM·임원)는 번호 체계를 "단락의 무게"로 읽음. 0.5짜리 단락은 본 단락보다 덜 중요해 보이지만, 실제로는 이 문서에서 가장 중요한 "UNCALLED를 제거 후보로 착각하지 말라"는 경고가 담긴 섹션.

(c) 수정안: 4로 승격하고 기존 4→5, 5→6, 6→7로 재번호.

④ 홈 상단 제목 중복

(a) topbar의 brand = "유광기 구조 지도"(index.html:926)와 홈 h1 = "유광기 서비스 구조 지도"(index.html:1100)가 문서 정체성 프롤로그를 사이에 끼고 두 번 등장. 그 사이에 프롤로그(≈3문단)가 들어가서 독자가 프롤로그를 다 읽고 h1을 만나면 "다시 제목부터?"로 읽힘.

(b) 1차에서 프롤로그를 추가하며 h1을 같이 재검토하지 않아 생긴 구조.

(c) 수정안: home-hero의 h1을 제거하거나, h1을 프롤로그 위로 올려 "제목 → 프롤로그 → bignums" 흐름으로 재배치.

⑤ "유광기 서비스 구조 지도" vs "유광기 구조 지도" 브랜드 불일치

(a) topbar brand는 "유광기 구조 지도", 홈 h1은 "유광기 서비스 구조 지도", 문서 제목 <title>(index.html:6)은 "유광기 서비스 구조 지도". 세 표기가 섞여 있음.

(b) 동일 문서의 같은 개념이 브라우저 탭·상단 바·본문에서 다르게 보임.

(c) 수정안: 하나로 통일.

4. 원칙 위반 잔존
신뢰성 — (A)-① 하드코딩 수치와 (B)-② 근거 줄 비대칭이 여기에 해당.
가시성 — sticky 바 충돌 외에, index.html:1686의 "... 외 {n}건"은 40건 이상일 때만 표시되어 테이블 모달에서 많은 엔드포인트가 있는 허브 테이블의 경우 "실제 몇 개를 감추고 있는지" 상단에서 알기 어려움. 상단에 총 수를 선언하고 40개로 잘랐음을 명시해야 함(현재 h3 텍스트에 총 수는 있지만 "표시 중 / 숨김" 구분이 없음).
직관성 — renderServices의 legend(index.html:1296–1299)에서 삼항 분기 c === 'green' ? 'green' : c === 'amber' ? 'amber' : c === 'red' ? 'red' : 'gray-400' 은 코드 입장에서 실수 여지. c === 'gray' ? 'gray-400' : c로 단순화 가능하며, 색상 정의가 팔레트와 1:1로 매칭되지 않음을 암시(팔레트엔 --gray-400만 있음)해 독자가 아닌 개발자에게는 혼란.
5. 다음 한 가지 수정 (ROI 최상)
하드코딩 수치 5곳을 데이터 바인딩으로 전환 (10–15분).

근거:

(b)의 완결성을 회복한다. "모든 하드코딩 수치를 치환했다"는 문서의 내부 주장과 실제 코드를 다시 일치시킴. 이게 깨져 있으면 1차 비평의 수정 자체가 불완전하다는 인상을 준다.
다음 스냅샷에서 517→519, 636→641 식으로 숫자가 바뀔 때 위 다섯 곳만 소리 없이 어긋난다. 데이터 바인딩 완결 상태가 유지되어야 이 문서가 "참조서"로 남는다.
가장 위험한 곳은 index.html:2305의 "유령 모델 2건"이다. 같은 값이 홈(index.html:1180)에서는 동적 바인딩이라, zombie_models가 추가되면 한 문서 안에서 서로 다른 숫자를 동시에 말하게 된다. 즉 회귀가 곧 모순으로 번지는 지점.
처치: 해당 5줄에서 문자열 보간(${META.total_schema_tables}, ${META.used_tables}, ${META.total_endpoints}, ${ENDPOINTS.filter(e => e.confidence === 'LOW').length}, ${DATA.diagnostics.zombie_models.length})으로 교체.

6. 수정 안 해도 괜찮은 것 (과도 폴리싱 방지)
DOMContentLoaded 리스너가 두 번 등록됨 (index.html:1022, 2348). 스타일적으로 깔끔하진 않지만 동작·성능에 영향 없음. 리팩터 ROI 낮음.
legend 삼항 분기의 장황함 (index.html:1296–1299). 위에서 지적했지만 사용자 측엔 보이지 않는 코드 품질. 다음 리팩터 라운드에서 번들로 처리.
About 섹션 1과 홈 프롤로그의 "폐쇄 세계" 반복. 같은 전제를 두 번 선언하는 건 오히려 "이 전제가 중요합니다"는 강조로도 기능하므로, 독자 대상(임원·PM)을 고려하면 제거보다 유지가 안전.